title: Test-project

pages:
  - name: Home        # required
    fileName: index   # default: name attribute with spaces replaced by dashes and all lower case.
    fileType: html    # default: html
    layout: default
    includes:
      - name: title
        data:         # This needs to be valid yaml as it will be pasted into a specific page data file.
          title: Das ist ein Titel
          description: Lorem Ipsum dolor sit amet das ist eine tolle Beschreibung dolor sit amet
      - name: list
        data:
          - eins
          - zwei
          - drei
